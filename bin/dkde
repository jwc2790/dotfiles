#!/bin/bash

read -p "Are you sure you want to destroy all containers and images? " -n 1 -r
echo # cleaner prompt
if [[ ! $REPLY =~ ^[Yy]$ ]]
then
    exit 1
fi
# Delete all containers
docker rm $(docker ps -a -q)
# Delete all images
docker rmi $(docker images -q)
